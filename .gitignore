.DS_Store

# Plaintext sources — must never be committed to a public repo. Unanchored, so
# it covers both the document at the root and the deck in powerbee/.
content/

# Browser-ready copy of the investor book, written by polishengine/pitch/pdf.mjs
# next to its assets so the relative font URLs resolve. Plaintext, like content/.
.preview.html

.vercel
